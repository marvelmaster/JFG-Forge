# Manifests

This directory will hold machine-readable provenance manifests for verified extraction runs. Each record should identify the source ROM, tool version, relevant offsets, output path, size, and SHA-256.
