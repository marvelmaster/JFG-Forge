# Local source assets

This directory is reserved for read-only local inputs such as SceneRipper
captures. Its contents are ignored by Git; only this policy file is committed.

No ROM image is stored in the public project. Tools require an explicit local
ROM path and validate this identity before extraction:

| Property | Value |
| --- | --- |
| SHA-1 | `493ced9008dbe932d6e91179b68e8630cf23a023` |
| Size | `33,554,432 bytes` |
| Byte order | `Z64 / Big Endian` |
