# Tests

This directory contains focused unit tests and research regressions for ROM
identity, model and texture parsing, animation sampling, runtime captures, JFG
Forge scene construction, playback, and glTF export.

Some integration tests use ignored local ROM, generated, or capture fixtures.
Run a focused module while developing, or use `unittest` discovery when all
local fixtures are available.
